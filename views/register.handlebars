<div class="flex">
    <div class="container">
        <h1>Make Berlin rent cheap again!</h1>
          <h2>Vestiges of the communist economy made Berlin one of the most economic options in Europe where was possible to have very high quality of life with very little income which benefited its inhabitants. But everytime exponentially faster agencies like Deutsche Wohnen are trying to change the type of population sometimes even using extortion methods, inflating the prices of community payments and rising the rent forcing people who can't not affort it to leave the city. This result of gentrification is ruining the appeal of Berlin.</h2>
        <h2 id="mainText">  </h2>
        <form method="post">
            <div class="sigButtons">
                {{#if error}}
                    <div class="error">Uh oh, something went wrong. Please try again!</div>
                {{/if}}
                <div class="input-text">
                    <label>
                        First Name: <input type="text" name="fname" required><br>
                    </label>
                </div>
                <div class="input-text">
                    <label>
                        Last Name: <input type="text" name="lname" required>
                    </label>
                </div>
                <div class="input-text">
                    <label>
                        Email: <input type="email" name="email" required>
                    </label>
                </div>
                <div class="input-text">
                    <label>
                        Password: <input type="password" name="password" required>
                    </label>
                </div>

                <input type="hidden" name="_csrf" value="{{csrfToken}}">
                <button class="btn" name="button">Sign Up</button>

                </div>
            </div>

        </form>

    </div>
</div>

<!-- {{> clientsidejs }} -->
